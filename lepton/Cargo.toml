[package]
name = "lepton"
version = "0.1.0"
edition = "2021"
license.workspace = true
repository.workspace = true
description = "Lepton host facade: SSR identity and token model bindings for Unified Field."
publish = false

[lints]
workspace = true

[dependencies]
lepton-host-adapter = { workspace = true, optional = true }

[features]
default = []
ssr = ["dep:lepton-host-adapter", "lepton-host-adapter/ssr"]
